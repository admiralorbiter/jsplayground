const config = {
    level: 0,
    scale: 20,
}

const wobbleSpeed = 8;
const wobbleDist = 0.07;

const playerXSpeed = 7;
const gravity = 30;
const jumpSpeed = 17;

export {wobbleSpeed, wobbleDist};
export {playerXSpeed, gravity, jumpSpeed};

export default config;