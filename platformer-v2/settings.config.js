/*
*   Config File: Settings that might need to be tweaked all in once place
*/

//Level Config
const config = {
    level: 0,
    scale: 20,
}

//Speeds for coin wobble
const wobbleSpeed = 8;
const wobbleDist = 0.07;

//Player Config
const playerXSpeed = 7;
const gravity = 30;
const jumpSpeed = 17;

export {wobbleSpeed, wobbleDist};
export {playerXSpeed, gravity, jumpSpeed};

export default config;